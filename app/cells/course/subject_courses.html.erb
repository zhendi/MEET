<% @subjects.each do |s| %>
  <div class="box_half">
    <h2 class="box_title"><%= s.name %></h2>
    <ul>
      <% s.courses.each do |c| %>
        <li>
        <%= link_to c.name, study_course_course_path(c) %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>
