<%= simple_form_for [@forum, @topic] do |f| %>
  <%= f.error_messages %> 

  <%= f.input :name %>
  <%= f.input :content, :as=>:text, :input_html=>{:rows=>8, :cols=>80} %>


  <%= f.button :submit, :class=>"ml140" %>
<% end %>

