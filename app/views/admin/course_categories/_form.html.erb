<%= form_for ([:admin, @course_category])  do  |f| %>
    <div class="field">
        <%= f.label :name  %>
        <%= f.text_field    :name %>
    </div>

    <div class="field">
        <%= f.submit %>
    </div>
<% end %>
