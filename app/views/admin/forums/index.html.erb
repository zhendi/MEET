<%= link_to t(:new, :scope=>[:common]), new_admin_forum_path, :class=>"miniBtn ml20 mt10" %>

<table>
  <tr>
    <th>ID</th>
    <th>Name</th>
  </tr>
  <% @forums.each do |forum| %>
    <tr>
      <td><%= forum.id %></td>
      <td><%= forum.name %></td>
    </tr>
  <% end %>
</table>
