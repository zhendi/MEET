<%= simple_form_for([:admin, @forum])   do  |f| %>
    <%= f.error_messages %>

    <%= f.input :name %>
    <%= f.input :description,   :as => :text, :input_html => { :rows => 6, :cols=>60 } %>

    <%= f.button    :submit,  :class=>"ml140" %>
<% end %>
