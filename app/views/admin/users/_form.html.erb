<%= simple_form_for([:admin, @admin_user]) do |f| %>
  <%= f.error_messages %>

  <div class="inputs">
    <%= f.input :email %>
    <%= f.input :created_at %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
