<%= link_to t(:new, :scope=>[:common]), new_admin_role_path, :class=>"miniBtn ml20 mt10" %>

<table>
  <tr>
    <th><%= t("ID") %></th>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @roles.each do |role| %>
  <tr>
    <td><%= role.id %></td>
    <td><%= role.name %></td>
    <td><%= link_to t('Show'), [:admin, role] %></td>
    <td><%= link_to t('Edit'), edit_admin_role_path(role) %></td>
    <td><%= link_to t('Destroy'), [:admin, role], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

