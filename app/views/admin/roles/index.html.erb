<%= button_to t(:new, :scope=>[:common]), new_admin_role_path, :method=>:get, :class=>"miniBtn left ml20 mt10" %>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @roles.each do |role| %>
  <tr>
    <td><%= role.name %></td>
    <td><%= link_to 'Show', [:admin, role] %></td>
    <td><%= link_to 'Edit', edit_admin_role_path(role) %></td>
    <td><%= link_to 'Destroy', [:admin, role], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

